#include  "main.h"
#include <stdlib.h>
/**
 * _abs - absolute value of an integer
 *@a: the interger to compute its absolute value.
 * Return: absvalue
 */
int _abs(int a)
{
	int absvalue;

	absvalue = abs(a);
	return (absvalue);
}
